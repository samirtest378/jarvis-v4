{
  "name": "jarvis-v4",
  "version": "4.1.1",
  "private": true,
  "scripts": {
    "setup": "npm ci --prefix frontend && npm ci --prefix desktop",
    "dev": "npm --prefix desktop run dev",
    "build": "npm --prefix desktop run build",
    "build:dir": "npm --prefix desktop run build:dir",
    "test:frontend": "npm --prefix frontend test",
    "test:desktop": "npm --prefix desktop test",
    "release:audit": "python3 scripts/release_audit.py",
    "release:audit:strict": "python3 scripts/release_audit.py --strict",
    "release:checksums": "python3 scripts/create_release_checksums.py"
  }
}
